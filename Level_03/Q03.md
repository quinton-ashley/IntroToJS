# Quiz 03

A student has written the following code segments and wants it . Check each code segment and explain what errors the student has made.

```js
let num = 1;

while num <= 10 {
	num + 1;
	alert('The number is now' + num);
}
```

For loops have three sections, what character is used to separate them?

What are the names of the three sections of a for loop?

The variable name `i` commonly used as a counter variable in for loops. What is `i` short for?

What does the ++ operator do?

What does the -- operator do?

Write a for loop that prints a countdown from 10 to 0. Use `console.log()` to print the numbers.

```js
// your code here
```
